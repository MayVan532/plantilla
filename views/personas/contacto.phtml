<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

<div class="personas-main-content" style="background:#ffffff;font-family:'Poppins',-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,'Helvetica Neue',Arial,sans-serif;color:#0b2238;">
  <?php if (isset($_GET['debugcms']) && $_GET['debugcms']=='1') { ?>
    <?php if (isset($this->debug_bloque30)) { ?>
      <pre style="background:#f8fafc;border:1px solid #e2e8f0;color:#0f172a;font-size:12px;padding:10px;border-radius:6px;margin:10px 0;white-space:pre-wrap;">B30: <?php echo htmlspecialchars(json_encode($this->debug_bloque30, JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES)); ?></pre>
    <?php } ?>
    <?php if (isset($this->debug_bloque31)) { ?>
      <pre style="background:#f8fafc;border:1px solid #e2e8f0;color:#0f172a;font-size:12px;padding:10px;border-radius:6px;margin:10px 0;white-space:pre-wrap;">B31: <?php echo htmlspecialchars(json_encode($this->debug_bloque31, JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES)); ?></pre>
    <?php } ?>
    <?php if (isset($this->debug_bloque32)) { ?>
      <pre style="background:#f8fafc;border:1px solid #e2e8f0;color:#0f172a;font-size:12px;padding:10px;border-radius:6px;margin:10px 0;white-space:pre-wrap;">B32: <?php echo htmlspecialchars(json_encode($this->debug_bloque32, JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES)); ?></pre>
    <?php } ?>
    <?php if (isset($this->contacto_social)) { ?>
      <pre style="background:#f8fafc;border:1px solid #e2e8f0;color:#0f172a;font-size:12px;padding:10px;border-radius:6px;margin:10px 0;white-space:pre-wrap;">contacto_social: <?php echo htmlspecialchars(json_encode($this->contacto_social, JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES)); ?></pre>
    <?php } ?>
  <?php } ?>

  <!-- Imagen principal -->
  <?php if (!empty($this->contacto_banner)) { ?>
  <div style="position: relative; max-width: 100%; overflow: hidden; margin-top: 0;">
    <div style="display: block; width: 100%;">
      <?php $href = isset($this->contacto_banner_link) ? trim((string)$this->contacto_banner_link) : ''; ?>
      <?php if ($href) { ?><a href="<?php echo htmlspecialchars($href); ?>" style="display:block;"><?php } ?>
      <img src="<?php echo htmlspecialchars($this->contacto_banner); ?>" 
           style="width: 100%; height: auto; display: block;">
      <?php if ($href) { ?></a><?php } ?>
    </div>
  </div>
  <?php } else { ?>
  <div style="position: relative; max-width: 100%; overflow: hidden; margin-top: 0; min-height: 320px; border-radius:12px; background:linear-gradient(180deg,#eef5fb 0%,#e6eef7 100%); border:1px solid #d9e5f0; box-shadow:0 6px 18px rgba(15, 23, 42, 0.05);"></div>
  <?php } ?>
  <div class="auto-container" style="max-width:1200px;margin:auto;padding:60px 20px;font-family:'Poppins',-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,'Helvetica Neue',Arial,sans-serif;">
    <div class="row" style="display:flex;flex-wrap:wrap;gap:40px;justify-content:center;align-items:flex-start;">

      <!-- Columna Imagen (solo si CMS provee) -->
      <?php if (!empty($this->contacto_secundario)) { ?>
      <div class="form-column contactimg1 col-lg-3 col-md-12 col-sm-12" style="text-align:center;">
        <?php $href2 = isset($this->contacto_secundario_link) ? trim((string)$this->contacto_secundario_link) : ''; ?>
        <?php if ($href2) { ?><a href="<?php echo htmlspecialchars($href2); ?>" style="display:inline-block;">
        <?php } ?>
        <img class="pronto" src="<?php echo htmlspecialchars($this->contacto_secundario); ?>" style="max-width:100%;border-radius:20px;">
        <?php if ($href2) { ?></a><?php } ?>
      </div>
      <?php } else { ?>
      <div class="form-column contactimg1 col-lg-3 col-md-12 col-sm-12" style="text-align:center;">
        <div style="width:100%; height:260px; background:#e9edf1; border-radius:20px;"></div>
      </div>
      <?php } ?>

      <!-- Formulario -->
      <div class="form-column col-lg-5 col-md-12 col-sm-12 formcontactores"
        style="background:#fff;padding:40px 30px;border-radius:16px;box-shadow:0 16px 40px rgba(6,25,41,0.08);border:1px solid rgba(30,198,227,0.08);flex:1;min-width:320px;">
        
        <div style="background:#1ec6e3;color:#fff;text-align:center;font-weight:700;font-size:32px;line-height:1.2;padding:18px 24px;border-radius:16px 16px 0 0;width:calc(100% + 60px);margin-left:-30px;margin-top:-36px;margin-bottom:20px;">
          Contáctanos
        </div>
        <div class="text" style="font-size:15px;line-height:22px;color:#3b4b59;margin-bottom:20px;">
          Para dudas y aclaraciones utiliza el formulario de contacto y un agente de servicio al cliente se contactará contigo.
        </div>

        <form method="post" id="frm_nuevo" class="custom-validation">
          <div class="form-group" style="margin-bottom:15px;">
            <input type="text" name="x1" id="x1" placeholder="Nombre" required
              style="width:100%;padding:12px 14px;border:1px solid #d9e2ea;border-radius:10px;font-size:15px;outline:none;transition:box-shadow .25s,border-color .25s;"
              onfocus="this.style.borderColor='#1ec6e3';this.style.boxShadow='0 0 0 4px rgba(30,198,227,0.18)';"
              onblur="this.style.borderColor='#d9e2ea';this.style.boxShadow='none';">
          </div>
          <div class="form-group" style="margin-bottom:15px;">
            <input type="email" name="x2" id="x2" placeholder="Correo electrónico" required
              style="width:100%;padding:12px 14px;border:1px solid #d9e2ea;border-radius:10px;font-size:15px;outline:none;transition:box-shadow .25s,border-color .25s;"
              onfocus="this.style.borderColor='#1ec6e3';this.style.boxShadow='0 0 0 4px rgba(30,198,227,0.18)';"
              onblur="this.style.borderColor='#d9e2ea';this.style.boxShadow='none';">
          </div>
          <div class="form-group" style="margin-bottom:15px;">
            <input type="text" name="x3" id="x3" placeholder="Teléfono" required
              style="width:100%;padding:12px 14px;border:1px solid #d9e2ea;border-radius:10px;font-size:15px;outline:none;transition:box-shadow .25s,border-color .25s;"
              onfocus="this.style.borderColor='#1ec6e3';this.style.boxShadow='0 0 0 4px rgba(30,198,227,0.18)';"
              onblur="this.style.borderColor='#d9e2ea';this.style.boxShadow='none';">
          </div>
          <div class="form-group" style="margin-bottom:15px;">
            <input type="text" name="x4" id="x4" placeholder="Asunto" required
              style="width:100%;padding:12px 14px;border:1px solid #d9e2ea;border-radius:10px;font-size:15px;outline:none;transition:box-shadow .25s,border-color .25s;"
              onfocus="this.style.borderColor='#1ec6e3';this.style.boxShadow='0 0 0 4px rgba(30,198,227,0.18)';"
              onblur="this.style.borderColor='#d9e2ea';this.style.boxShadow='none';">
          </div>
          <div class="form-group" style="margin-bottom:25px;">
            <textarea name="x5" id="x5" placeholder="Mensaje"
              style="width:100%;padding:12px 14px;border:1px solid #d9e2ea;border-radius:10px;font-size:15px;height:110px;resize:none;outline:none;transition:box-shadow .25s,border-color .25s;"
              onfocus="this.style.borderColor='#1ec6e3';this.style.boxShadow='0 0 0 4px rgba(30,198,227,0.18)';"
              onblur="this.style.borderColor='#d9e2ea';this.style.boxShadow='none';"></textarea>
          </div>

          <div class="form-group" style="text-align:center;">
            <button class="theme-btn btn-style-three" type="submit" name="Submit"
              style="background:linear-gradient(90deg,#1ec6e3 0%,#12a9c1 100%);color:#fff;padding:12px 40px;border:none;border-radius:0;font-size:16px;font-weight:700;cursor:pointer;box-shadow:0 12px 26px rgba(30,198,227,0.35);transition:transform .2s ease,box-shadow .2s ease;"
              onmouseover="this.style.transform='translateY(-2px)';this.style.boxShadow='0 16px 32px rgba(30,198,227,0.45)';"
              onmouseout="this.style.transform='none';this.style.boxShadow='0 12px 26px rgba(30,198,227,0.35)';">
              Enviar
            </button>
          </div>
        </form>
      </div>

      <!-- Redes Sociales y Contacto -->
      <div class="sidebar-column col-lg-4 col-md-12 col-sm-12" style="flex:1;min-width:300px;">
        <div class="inner-column" style="text-align:left;background:#ffffff;border:1px solid #e5edf7;border-radius:22px;box-shadow:0 12px 30px rgba(15,23,42,0.09);padding:22px 22px 20px;">

          <!-- Redes Sociales (solo si CMS provee) -->
          <?php 
            $raw = [];
            if (!empty($this->contacto_social_links) && is_array($this->contacto_social_links)) { $raw = array_merge($raw, $this->contacto_social_links); }
            if (!empty($this->contacto_social) && is_array($this->contacto_social)) { $raw = array_merge($raw, $this->contacto_social); }
            // Fallback: reutiliza redes normalizadas del footer si no llegan por el bloque
            if (empty($raw) && !empty($this->footer_social) && is_array($this->footer_social)) {
              foreach ($this->footer_social as $k => $v) { if (is_string($v) && trim($v) !== '') { $raw[] = $v; } }
            }
            // Tomar preferentemente 'link' de cada elemento del CMS (como en FAQ)
            $vals = [];
            foreach ($raw as $r) {
              if (is_array($r)) {
                $lnk = (string)($r['link'] ?? '');
                if ($lnk==='') { $lnk = (string)($r['contenido'] ?? ''); }
                if ($lnk==='') { $lnk = (string)($r['valor'] ?? ''); }
                if ($lnk==='') { $lnk = (string)($r['url'] ?? ''); }
                if ($lnk!=='') { $vals[] = $lnk; }
              } elseif (is_object($r)) {
                $lnk = (string)($r->link ?? '');
                if ($lnk==='') { $lnk = (string)($r->contenido ?? ''); }
                if ($lnk==='') { $lnk = (string)($r->valor ?? ''); }
                if ($lnk==='') { $lnk = (string)($r->url ?? ''); }
                if ($lnk!=='') { $vals[] = $lnk; }
              } else { $vals[] = (string)$r; }
            }
            $vals = array_values(array_filter(array_map('trim',$vals)));
            // Construir redes con el mismo estilo de FAQ
            $social = [];
            $seen = [];
            foreach ($vals as $u) {
              $lu = strtolower(trim($u));
              // ignorar valores que son clases de iconos u otros no-URL
              if (strpos($lu,'mailto:')===0 || strpos($lu,'tel:')===0 || strpos($lu,'wa.me/')!==false || strpos($lu,'whatsapp')!==false) { continue; }
              if (strpos($lu,'fa-')!==false || strpos($lu,'fab ')===0 || strpos($lu,'fas ')===0) { continue; }
              // Normalizar handles o rutas parciales
              if (!(stripos($lu,'http://')===0 || stripos($lu,'https://')===0 || stripos($lu,'www.')===0)) {
                if ($lu[0] === '@') { // posible handle tiktok/instagram
                  // sin plataforma no podemos inferir, así que continúa
                  // lo reintentaremos si otra entrada trae dominio
                  continue;
                }
              }
              $platform = null; $style=''; $icon='';
              if (strpos($lu,'facebook')!==false) { $platform='facebook'; $style='background:#1877f2;'; $icon='fab fa-facebook-f'; }
              elseif (strpos($lu,'instagram')!==false) { $platform='instagram'; $style='background:linear-gradient(45deg,#feda75,#d62976,#962fbf,#4f5bd5);'; $icon='fab fa-instagram'; }
              elseif (strpos($lu,'tiktok')!==false) { $platform='tiktok'; $style='background:#000;'; $icon='fab fa-tiktok'; }
              elseif (strpos($lu,'youtube')!==false || strpos($lu,'youtu.be')!==false) { $platform='youtube'; $style='background:#FF0000;'; $icon='fab fa-youtube'; }
              if ($platform) {
                $url = trim($u);
                // completar esquema si falta
                if (stripos($url,'http') !== 0) { $url = 'https://' . ltrim($url,'/'); }
                // normalizar tiktok/instagram si vienen como @handle
                if ($platform==='tiktok' && strpos($url,'@')===0) { $url = 'https://www.tiktok.com/'.$url; }
                if ($platform==='instagram' && strpos($url,'@')===0) { $url = 'https://www.instagram.com/'.ltrim($url,'@').'/'; }
                // deduplicar por plataforma conservando el primero válido
                if (!isset($seen[$platform])) {
                  $seen[$platform] = true;
                  $social[] = ['url'=>$url,'style'=>$style,'icon'=>$icon];
                }
              }
            }
          ?>
          <?php if (!empty($social)) { ?>
          <div class="social-widget" style="margin-bottom:22px; padding:18px 22px 18px; border-radius:18px; background:linear-gradient(135deg,#e0f7ff 0%,#e3f2fd 40%,#f5fbff 100%); border:1px solid #dbeafe; display:flex; flex-direction:column; align-items:center; gap:18px;">
            <h2 style="margin:0;font-size:15px;font-weight:800;color:#0ea5a6;text-align:center;letter-spacing:2px;text-transform:uppercase;">Síguenos en</h2>
            <ul style="list-style:none;padding:0;margin:0;display:flex;justify-content:center;align-items:center;gap:18px;">
              <?php foreach ($social as $s) { ?>
              <li style="display:flex;">
                <a href="<?php echo htmlspecialchars($s['url']); ?>" target="_blank" style="display:flex;align-items:center;justify-content:center;width:46px;height:46px;<?php echo $s['style']; ?> color:#fff;border-radius:9999px;font-size:22px;text-decoration:none;box-shadow:0 8px 20px rgba(15,23,42,0.18);transition:transform .22s ease, box-shadow .22s ease;" onmouseover="this.style.transform='translateY(-3px) scale(1.06)';this.style.boxShadow='0 14px 26px rgba(15,23,42,0.30)'" onmouseout="this.style.transform='translateY(0) scale(1)';this.style.boxShadow='0 8px 20px rgba(15,23,42,0.18)';">
                  <i class="<?php echo $s['icon']; ?>" style="line-height:1;"></i>
                </a>
              </li>
              <?php } ?>
            </ul>
          </div>
          <?php } else { ?>
          <div class="social-widget" style="margin-bottom:22px; padding:18px 22px 18px; border-radius:18px; background:linear-gradient(135deg,#e0f7ff 0%,#e3f2fd 40%,#f5fbff 100%); border:1px solid #dbeafe; display:flex; flex-direction:column; align-items:center; gap:18px;">
            <h2 style="margin:0;font-size:15px;font-weight:800;color:#0ea5a6;text-align:center;letter-spacing:2px;text-transform:uppercase;">Síguenos en</h2>
            <ul class="social-icon-two" style="display:flex;gap:18px;list-style:none;padding:0;margin:0;align-items:center;justify-content:center;">
              <?php for($i=0;$i<4;$i++){ ?>
              <li><span style="display:inline-block;width:46px;height:46px;border-radius:9999px;background:#e5edf7;border:1px solid #d1d9e8;box-shadow:0 6px 14px rgba(15,23,42,0.12);"></span></li>
              <?php } ?>
            </ul>
          </div>
          <?php } ?>

          <!-- Soporte (derivado de links del CMS) -->
          <?php 
            $linksRaw = [];
            if (!empty($this->contacto_social_links) && is_array($this->contacto_social_links)) { $linksRaw = array_merge($linksRaw, $this->contacto_social_links); }
            if (!empty($this->contacto_social) && is_array($this->contacto_social)) { $linksRaw = array_merge($linksRaw, $this->contacto_social); }
            // Usar 'link' de cada fila (igual que en FAQ)
            $links = [];
            foreach ($linksRaw as $r) {
              if (is_array($r)) {
                $lnk = (string)($r['link'] ?? '');
                if ($lnk==='') { $lnk = (string)($r['contenido'] ?? ''); }
                if ($lnk==='') { $lnk = (string)($r['valor'] ?? ''); }
                if ($lnk==='') { $lnk = (string)($r['url'] ?? ''); }
                if ($lnk!=='') { $links[] = $lnk; }
              } elseif (is_object($r)) {
                $lnk = (string)($r->link ?? '');
                if ($lnk==='') { $lnk = (string)($r->contenido ?? ''); }
                if ($lnk==='') { $lnk = (string)($r->valor ?? ''); }
                if ($lnk==='') { $lnk = (string)($r->url ?? ''); }
                if ($lnk!=='') { $links[] = $lnk; }
              } else { $links[] = (string)$r; }
            }
            $tel = '';$mail='';$wa='';$waLink='';
            // Detección directa por tipo de icono si vienen como arrays del CMS
            foreach ($linksRaw as $r) {
              $tipo = '';$lnk='';
              if (is_array($r)) {
                $tipo = strtolower((string)($r['tipo'] ?? ''));
                $lnk  = (string)($r['link'] ?? '');
                if ($lnk==='') { $lnk = (string)($r['contenido'] ?? ''); }
                if ($lnk==='') { $lnk = (string)($r['valor'] ?? ''); }
                if ($lnk==='') { $lnk = (string)($r['url'] ?? ''); }
                if ($lnk==='') { $lnk = (string)($r['texto'] ?? ''); }
                if ($lnk==='') { $lnk = (string)($r['titulo'] ?? ''); }
              } elseif (is_object($r)) {
                $tipo = strtolower((string)($r->tipo ?? ''));
                $lnk  = (string)($r->link ?? '');
                if ($lnk==='') { $lnk = (string)($r->contenido ?? ''); }
                if ($lnk==='') { $lnk = (string)($r->valor ?? ''); }
                if ($lnk==='') { $lnk = (string)($r->url ?? ''); }
                if ($lnk==='') { $lnk = (string)($r->texto ?? ''); }
                if ($lnk==='') { $lnk = (string)($r->titulo ?? ''); }
              } else { continue; }
              if (!$tel && strpos($tipo,'phone')!==false && $lnk!=='') { $tel = preg_replace('/[^0-9+]/','',$lnk); }
              if (!$mail && strpos($tipo,'envelope')!==false && $lnk!=='') { $mail = trim(preg_replace('/^mailto:/i','',$lnk)); }
              if (!$wa && strpos($tipo,'whatsapp')!==false && $lnk!=='') { $wa = preg_replace('/\D+/','',$lnk); $waLink = $lnk; }
            }
            foreach ($links as $u) {
              $lu = strtolower(trim($u));
              // Teléfono: soporta tel:, o puro con + y dígitos
              if (!$tel) {
                if (strpos($lu,'tel:')===0) { $tel = preg_replace('/^tel:\s*/i','', $u); }
                elseif (preg_match('/^\+?[0-9][0-9\s\-()]{6,}$/', $u)) { $tel = preg_replace('/[^0-9+]/','', $u); }
              }
              // Email: soporta mailto:, o puro con @
              if (!$mail) {
                if (strpos($lu,'mailto:')===0) { $mail = preg_replace('/^mailto:\s*/i','', $u); }
                elseif (preg_match('/^[\w.+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$/', trim($u))) { $mail = trim($u); }
              }
              // WhatsApp: wa.me, api.whatsapp, whatsapp.com, o query ?phone=
              if (!$wa) {
                if (strpos($lu,'wa.me/')!==false || strpos($lu,'api.whatsapp')!==false || strpos($lu,'whatsapp.com')!==false) {
                  $wa = preg_replace('/\D+/','', $u);
                  $waLink = trim($u);
                } elseif (strpos($lu,'phone=')!==false) {
                  if (preg_match('/phone=([0-9]+)/i', $u, $m)) { $wa = $m[1]; }
                  $waLink = trim($u);
                }
              }
            }
            // Normalizar finales
            $tel = trim($tel);
            if ($tel !== '') { $tel = preg_replace('/[^0-9+]/','', $tel); if ($tel[0] !== '+') { $tel = '+'.$tel; } }
            $mail = trim($mail);
            $wa = trim($wa);
            $hasSupport = $tel || $mail || $wa;
          ?>
          <?php if ($hasSupport) { ?>
          <div class="support-widget" style="margin-top:20px; text-align:left; padding:8px 2px 0;">
            <span style="display:block; margin-bottom:10px; font-size:11px; letter-spacing:2px; color:#0ea5a6; font-weight:800; text-transform:uppercase;">
              SOPORTE TECNICO Y ATENCIÓN A CLIENTES
            </span>
            <h2 style="margin:0 0 16px; font-size:24px; font-weight:800; color:#0f172a;">
              Llámanos
            </h2>

            <div style="display:flex; flex-direction:column; gap:14px; margin-bottom:18px;">
              <?php if ($mail) { ?>
              <div class="info" style="display:flex; align-items:center; font-size:16px; color:#0f172a;">
                <span style="flex-shrink:0; width:44px; height:44px; border-radius:50%; background:linear-gradient(135deg,#0f3c8f,#102a6b); display:flex; align-items:center; justify-content:center; margin-right:14px; box-shadow:0 6px 14px rgba(15,23,42,0.25);">
                  <i class="fas fa-envelope" style="font-size:18px; color:#ffffff;"></i>
                </span>
                <a href="<?php echo 'mailto:'.htmlspecialchars($mail); ?>" style="color:#0f172a; font-weight:700; text-decoration:none;">
                  <?php echo htmlspecialchars($mail); ?>
                </a>
              </div>
              <?php } ?>

              <?php if ($tel) { ?>
              <div class="info" style="display:flex; align-items:center; font-size:16px; color:#0f172a;">
                <span style="flex-shrink:0; width:44px; height:44px; border-radius:50%; background:linear-gradient(135deg,#00bcd4,#00acc1); display:flex; align-items:center; justify-content:center; margin-right:14px; box-shadow:0 6px 14px rgba(0,172,193,0.35);">
                  <i class="fas fa-phone-alt" style="font-size:18px; color:#ffffff;"></i>
                </span>
                <a href="<?php echo 'tel:'.htmlspecialchars($tel); ?>" style="color:#0f172a; font-weight:700; text-decoration:none;">
                  <?php echo htmlspecialchars($tel); ?>
                </a>
              </div>
              <?php } ?>
            </div>

            <?php if ($wa) { 
                  $wl = $waLink ?: ('https://wa.me/'.$wa);
                  if (stripos($wl,'http') !== 0) {
                    if (stripos($wl,'send?') !== false) { $wl = 'https://api.whatsapp.com/' . ltrim($wl,'/'); }
                    else { $wl = 'https://' . ltrim($wl,'/'); }
                  }
            ?>
              <a href="<?php echo htmlspecialchars($wl); ?>" target="_blank" rel="noopener"
                 style="display:flex;align-items:center;justify-content:center;gap:8px;width:100%;background:linear-gradient(90deg,#00c853,#00e676);color:#fff;padding:11px 18px;border-radius:9999px;text-decoration:none;font-weight:800;font-size:15px;box-shadow:0 10px 22px rgba(0,200,83,0.45);transition:transform .2s ease, box-shadow .2s ease;">
                <i class="fab fa-whatsapp" style="font-size:18px;color:#fff;"></i>
                Contáctanos Ahora
              </a>
            <?php } ?>
          </div>
          <?php } else { ?>
          <div class="support-widget" style="margin-top: 20px; text-align:left; padding:6px 2px 0;">
            <span style="display:block; margin-bottom:8px; font-size:11px; letter-spacing:2px; color:#0ea5a6; font-weight:800; text-transform:uppercase;">SOPORTE TECNICO Y ATENCIÓN A CLIENTES</span>
            <div style="height:16px; width:180px; background:#e9edf1; border-radius:6px; margin-bottom:10px;"></div>
            <div style="height:16px; width:220px; background:#e9edf1; border-radius:6px; margin-bottom:10px;"></div>
            <div style="height:16px; width:200px; background:#e9edf1; border-radius:6px;"></div>
          </div>
          <?php } ?>
        </div>
      </div>
    </div>
  </div>
</div>